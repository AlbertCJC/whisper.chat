{
  "name": "whisper-web",
  "version": "2.0.0",
  "type": "module",
  "description": "Whisper anonymous chat — Vite-based single-page front-end",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:ui": "npx playwright test",
    "test:ui:headed": "npx playwright test --headed"
  },
  "dependencies": {
    "dompurify": "^3.4.10",
    "socket.io-client": "^4.2.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.0",
    "vite": "^8.0.16"
  }
}
